[extend]
useDefault = true

[[allowlists]]
description = "Gameplay tag values in the reference taxonomy, not credentials. Other content in this file remains scanned."
condition = "AND"
paths = ['''^scripts/tags-data\.json$''']
regexTarget = "match"
regexes = ['''Skills\.Key\.[A-Za-z0-9_]+''']

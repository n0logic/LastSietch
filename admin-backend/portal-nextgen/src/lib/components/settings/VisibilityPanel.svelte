<script>
  // Directory visibility, moved off the Mailbox. The card is the shipped one:
  // it owns its own load, its optimistic toggle and its revert, so this is a
  // placement wrapper and deliberately nothing more.
  import ProfileVisibilityCard from '$lib/components/guild/ProfileVisibilityCard.svelte';
</script>

<ProfileVisibilityCard />

{
  "__meta__": {
    "purpose": "Minimal operator-managed per-unit volume reference for dune-storage-write.py. No extracted volume dataset is distributed.",
    "resolution_order": "volume_override (DB, authoritative) -> 0 for currency/zero-vol -> this VMAP -> UNKNOWN (allow move, audit 'volume_unverified', never a hard block)",
    "refresh": "Re-extract from the game paks on EVERY Funcom content update. A missing/stale entry is fail-open (UNKNOWN), so the gate never wrongly blocks the player's own item; it only stops relying on the VMAP lower bound.",
    "note": "Keys prefixed with __ are metadata and are never matched as template ids. Populate real per-unit volumes from the pak VMAP; until then every non-override, non-currency template resolves UNKNOWN."
  },
  "SolarisCoin": 0
}
